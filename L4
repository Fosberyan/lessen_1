a = int(input('Введите число больше 0 - '))
maxi = a % 10
num = a

while num > 0:
    d = num % 10
    if d > maxi:
        maxi = d
        if d == 9:
            break

    num = num // 10
    print(num)

print(f"Наибольшая цифра в числе {a} равна {maxi}")

